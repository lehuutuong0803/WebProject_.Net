
@{
    ViewBag.Title = "StuffList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Start Stuff -->
<div class="stuff-box">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="heading-title text-center">
                    <h2>Fouders</h2>
                    <p>6 là đầu bếp hàng đầu cũng như là fouder của chuỗi nhà hàng!</p>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-4 col-sm-6">
                <div class="our-team">
                    <div class="pic">
                        <img src="~/images/stuff-img-01.jpg">
                        <ul class="social">
                            <li><a href="#" class="fa fa-facebook"></a></li>
                        </ul>
                    </div>
                    <div class="team-content">
                        <h3 class="title">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 6)
                                {<p>
                                        @item.NameStuff
                                    </p>
                                }
                            }
                                            </h3>
                        <span class="post">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 6)
                                {<p>
                                      Id: @item.Id
                                    </p>
                                }
                            }
                        </span>
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="our-team">
                    <div class="pic">
                        <img src="~/images/stuff-img-02.jpg">
                        <ul class="social">
                            <li><a href="#" class="fa fa-facebook"></a></li>
                            <li><a href="#" class="fa fa-google-plus"></a></li>
                            <li><a href="#" class="fa fa-instagram"></a></li>
                            <li><a href="#" class="fa fa-linkedin"></a></li>
                        </ul>
                    </div>
                    <div class="team-content">
                        <h3 class="title">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 1)
                                {<p>
                                        @item.NameStuff
                                    </p>
                                }
                            }
                                            </h3>
                        <span class="post">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 1)
                                {<p>
                                        Id: @item.Id
                                    </p>
                                }
                            }
                                            </span>
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="our-team">
                    <div class="pic">
                        <img src="~/images/stuff-img-03.jpg">
                        <ul class="social">
                            <li><a href="#" class="fa fa-facebook"></a></li>

                        </ul>
                    </div>
                    <div class="team-content">
                        <h3 class="title">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 2)
                                {<p>
                                        @item.NameStuff
                                    </p>
                                }
                            }
                                            </h3>
                        <span class="post">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 2)
                                {<p>
                                        Id: @item.Id
                                    </p>
                                }
                            }
                                            </span>
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="our-team">
                    <div class="pic">
                        <img src="~/images/stuff-img-04.jpg">
                        <ul class="social">
                            <li><a href="#" class="fa fa-facebook"></a></li>

                        </ul>
                    </div>
                    <div class="team-content">
                        <h3 class="title">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 3)
                                {<p>
                                        @item.NameStuff
                                    </p>
                                }
                            }
                                            </h3>
                        <span class="post">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 3)
                                {<p>
                                        Id: @item.Id
                                    </p>
                                }
                            }
                                            </span>
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="our-team">
                    <div class="pic">
                        <img src="~/images/stuff-img-05.jpg">
                        <ul class="social">
                            <li><a href="#" class="fa fa-facebook"></a></li>

                        </ul>
                    </div>
                    <div class="team-content">
                        <h3 class="title">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 4)
                                {<p>
                                        @item.NameStuff
                                    </p>
                                }
                            }
                                            </h3>
                        <span class="post">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 4)
                                {<p>
                                        Id: @item.Id
                                    </p>
                                }
                            }
                                            </span>
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="our-team">
                    <div class="pic">
                        <img src="~/images/stuff-img-06.jpg">
                        <ul class="social">
                            <li><a href="#" class="fa fa-facebook"></a></li>
                        </ul>
                    </div>
                    <div class="team-content">
                        <h3 class="title">
                            @foreach (var item in Model)
                            {
                                if (item.Id == 5)
                                {<p>
                                        @item.NameStuff
                                    </p>
                                }
                            }
                            <span class="post">
                                @foreach (var item in Model)
                                {
                                    if (item.Id == 5)
                                    {<p>
                                            Id: @item.Id
                                        </p>
                                    }
                                }
                            </span>
</div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- End Stuff -->
<!-- Start Customer Reviews -->
<div class="customer-reviews-box">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="heading-title text-center">
                    <h2>Đánh giá của người dùng</h2>
                    <p>Là các review được thu nhặt ngẫu hiên của người dùng đánh giá!</p>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-8 mr-auto ml-auto text-center">
                <div id="reviews" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner mt-4">
                        <div class="carousel-item text-center active">
                            <div class="img-box p-1 border rounded-circle m-auto">
                                <img class="d-block w-100 rounded-circle" src="~/images/quotations-button.png" alt="">
                            </div>
                            <h5 class="mt-4 mb-0"><strong class="text-warning text-uppercase">Phận Nhật Vạm</strong></h5>
                            <h6 class="text-dark m-0">CEO BIDV</h6>
                            <p class="m-0 pt-3">Tuyệt vời xứng đáng nhânnj 10 sao mê-chơ-lin</p>
                        </div>
                        <div class="carousel-item text-center">
                            <div class="img-box p-1 border rounded-circle m-auto">
                                <img class="d-block w-100 rounded-circle" src="~/images/quotations-button.png" alt="">
                            </div>
                            <h5 class="mt-4 mb-0"><strong class="text-warning text-uppercase">Phạm Nhật Vượng</strong></h5>
                            <h6 class="text-dark m-0">Nhân viên văn phòng</h6>
                            <p class="m-0 pt-3">Giá cả đi đôi với chất lượng.</p>
                        </div>

                    </div>
                    <a class="carousel-control-prev" href="#reviews" role="button" data-slide="prev">
                        <i class="fa fa-angle-left" aria-hidden="true"></i>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#reviews" role="button" data-slide="next">
                        <i class="fa fa-angle-right" aria-hidden="true"></i>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- End Customer Reviews -->